import { CourseProgressPipe } from './course-progress-pipe';

describe('CourseProgressPipe', () => {
    it('create an instance', () => {
        const pipe = new CourseProgressPipe();
        expect(pipe).toBeTruthy();
    });
});
